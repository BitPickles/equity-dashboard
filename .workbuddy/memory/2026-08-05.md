# 2026-08-05

## 每日数据同步（自动化 automation-1785847865078）
- update-prices.py：27 协议全部更新（pancakeswap 首次 SSL 失败，用 `--protocol pancakeswap` 重试成功）
- rebuild-daily.py + sync-all-protocols-from-snapshots.py（26 snapshots）均正常
- validate.py：0 errors, 0 warnings
- 提交 `254afa62 chore(daily): 每日数据同步` 推送 origin/dev（36 files）
- 经验：CoinGecko 429 限流时 update-prices.py 支持 `--protocol <name>` 单独重试；临时脚本（.calc-aster.py 等）在根目录未跟踪，提交时只 `git add data/`。
