# {协议名} — 协议口径判定书（内部文档）

> **内部资料，不对外展示**。本文件是接管 Agent 的快速上手手册：理解该协议「收入 → 股东回报」如何判定、数据从哪来、口径边界在哪。
> 最近更新：2026-08-04

---

## 一、协议档案

| 项 | 值 |
|---|---|
| 名称 / Ticker | {名称} / {Ticker} |
| 实体类型 | `{platform_token | application}` |
| 市值（as_of 2026-08-04） | ~${市值} |
| 股东回报率 | ~{回报率}%（股息 {股息}% + 回购 {回购}%） |
| 置信度 | {high | medium | low} |
| 数据源优先级 | 链上 > 官方治理 > 估算 > DefiLlama |

## 二、收入判定（核心）

**Boss 定稿口径：{一句话核心原则}**

```
{公式}
```

| 计入项 | 说明 | 数据源 |
|---|---|---|
| {收入项 1} | {说明} | {数据源} |
| {收入项 2} | {说明} | {数据源} |

| 不计入 | 原因 |
|---|---|
| {排除项 1} | {原因} |

## 三、股东回报拆分

| 机制 | 类型 | 占比 |
|---|---|---|
| {机制 1} | `{yield | destroy | buyback}`（股息/回购） | {占比}% |

## 四、关键计算逻辑（adapter.py）

`data/protocols/{id}/adapter.py` → `build_snapshot(proto_dir)`：

1. {步骤 1}
2. {步骤 2}

## 五、数据管道（data_pipeline）

| 数据 | 频率 | 脚本 |
|---|---|---|
| {数据 1} | {频率} | {脚本} |

## 六、历史验证与注意点

- **⚠️ {注意点 1}**：{说明}
- {注意点 2}

## 七、判定书出处

- 判定书总表：`docs/protocol-revenue-recognition.md` §{编号}
- 配置文件：`data/protocols/{id}/config.json`（revenue_recognition 字段）
- 数据维护说明：`data/protocols/{id}/README.md`（如有）
