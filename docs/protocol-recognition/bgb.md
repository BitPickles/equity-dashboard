# BGB — 协议口径判定书（内部文档）

> **内部资料，不对外展示**。本文件是接管 Agent 的快速上手手册：理解 BGB 协议「收入 → 股东回报」如何判定、数据从哪来、口径边界在哪。
> 最近更新：2026-08-04（⚠️ 重大修正：回购额从 $520M → $27.7M）

---

## 一、协议档案

| 项 | 值 |
|---|---|
| 名称 / Ticker | Bitget Token / BGB |
| 实体类型 | `platform_token`（平台币，已与 Bitget 平台切割） |
| 市值（as_of 2026-08-04） | ~$1.12B（CMC 实时，price $1.61 × 6.98 亿流通） |
| 股东回报率 | ~**2.47%**（股息 0% + 回购 2.47%） |
| 置信度 | low |
| 数据源优先级 | 官方公告（季度销毁）> 链上销毁地址 > DefiLlama |

## 二、收入判定（核心）

**Boss 定稿口径：季度回购销毁（2025-11 起挂钩 Morph 链 Gas 费）**

```
收入 = 2026 实测年化 = (Q1 $807 万 + Q2 $578 万) × 2 = $2,770 万/年
回报率 = $2,770 万 ÷ 市值 $11.2 亿 = 2.47%
```

| 计入项 | 说明 | 数据源 |
|---|---|---|
| 季度回购销毁（Morph 链费挂钩） | 2025-11 起机制：「利润 20%」→ 挂钩 Morph 链 Gas 费（boost ~1100-1400×）；季度执行、次季初完成、每次公布数量 + 链上记录 | 官方公告 + 链上销毁地址 |

| 不计入 | 原因 |
|---|---|
| 打新（Launchpad） | Boss 确认：Bitget 基本没有打新，不计入 |
| 2025 年一次性大额销毁（Q1/Q2 ~3000 万枚/季） | 已一次性发生，非持续机制，不可年化（旧口径错误根源） |

## 三、股东回报拆分

| 机制 | 类型 | 占比 |
|---|---|---|
| 季度回购销毁（Morph 链费挂钩） | `buyback`（回购销毁） | 2.47% |

## 四、关键计算逻辑（adapter.py）

`data/protocols/bgb/adapter.py` → `build_snapshot(proto_dir)`：

1. 常量 `ANNUAL_BURN_USD = $2,770 万`（2026 Q1+Q2 实测年化，Agent 2026-08-04 调研）
2. 市值取 all-protocols.json `market_cap_usd`（2026-08-04 更新为 CMC 实时 $1.12B）
3. `yield_pct = burn_usd / mcap × 100% = 2.47%`
4. 毛利 = 净利 = 收入（平台币无 LP 分润成本；无持续增发——2024-12 一次性销毁 8 亿后总供应 12 亿，100% 全流通）
5. 股东回报 by_mechanism = [{mechanism: "季度回购销毁（Morph 链费挂钩）", type: "buyback", usd: $27.7M, yield: 2.47%}]
6. 派生估值：pe = ps = mcap/burn ≈ 40.5x，payout_ratio = 1.0

## 五、数据管道（data_pipeline）

| 数据 | 频率 | 脚本 |
|---|---|---|
| 官方：AI 抓季度销毁公告（数量 + 日期） | 季度 + 日复核 | ai-watch-official.py |
| 链上：销毁地址复核（0x000...dEaD 季度 + 0x000...000 首次） | 日频 | update-bgb-tev.py |

## 六、历史验证与注意点

- **⚠️ 2026-08-04 重大修正（Boss 质疑）**：旧口径用 2025 Q1/Q2 销毁额（$1.2-1.4 亿/季度）年化 → $520M/年 → 回报率 27.54%**虚高 18 倍**。2025-11 机制已从「利润 20%」改为挂钩 Morph 链 Gas 费，2026 Q1/Q2 各只销毁 ~300 万枚（~$578-807 万）。修正后年化 $2,770 万、回报率 2.47%
- **⚠️ 销毁源头钱包未公开**：Bitget 未公开 buyback executor 钱包，无法链上验证销毁的 BGB 是市场真金白银买回还是公司金库自持转账（类 JustLend pocket-to-pocket 嫌疑）→ confidence=low
- **⚠️ 机制可能再变**：Morph 链费挂钩的销毁额随链上 Gas 活跃度波动，需持续监控季度公告
- **供应历史**：2024-12 一次性销毁 8 亿枚（总供应 40%，$5B+）→ 总供应 20亿 → 12 亿，100% 全流通
- **价格波动大**：ATH $8.45（2025），现 $1.61，跌 80%——市值变化会显著影响回报率
- **不可仅靠 DefiLlama**：CEX 数据不透明，需官方公告 + 链上销毁地址双源

## 七、判定书出处

- 判定书总表：`docs/protocol-revenue-recognition.md` §4（Boss 2026-08-02 定稿 + 2026-08-04 修正）
- 配置文件：`data/protocols/bgb/config.json`（revenue_recognition 字段）
- 数据维护说明：`data/protocols/bgb/README.md`（如有）
